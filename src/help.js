/* ═══════════════════════════════════════════════════
   mov-showcase — the command surface the Usage tab shows

   Generated by verify/help-from-mov.py from `mov -h` and `mov <command> -h`.
   Nobody types this file. The other views are held to mov's wording by
   verify/mov-strings.json; this one is held to it by construction.

   Regenerate after a release that changes the command surface:
     python verify/help-from-mov.py
   ═══════════════════════════════════════════════════ */

window.MOV = window.MOV || {};

window.MOV.HELP = {
  "version": "mov 2.19.2 (8fd03d7)",
  "root": " Usage: mov [OPTIONS] COMMAND [ARGS]...\n\n Profile-driven Azure environment control. Declare it as JSON, deploy it, tear it\n down, audit it. Commands that touch Azure act on the workspace above and point az\n at it first.\n\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --workspace     -w         <path>  Workspace directory or mov.workspace.json,   │\n│                                    given before the subcommand: `mov -w <path>  │\n│                                    up prod`. Defaults to the one `mov workspace │\n│                                    use` chose.                                  │\n│ --json                             Emit machine-readable JSON only.             │\n│ --subscription             <str>   Act on this subscription instead of the one  │\n│                                    the workspace pins. Given before the         │\n│                                    subcommand. Tenant-scoped commands           │\n│                                    (directory, rbac) still run in the           │\n│                                    workspace's tenant.                          │\n│ --version       -v,-V              Print the version and exit.                  │\n│ --help          -h                 Show this message and exit.                  │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Setting up ────────────────────────────────────────────────────────────────────┐\n│ setup         Create a workspace, interactively: `setup [path]`.                │\n│ init          Scaffold mov.workspace.json without prompting, for scripts and    │\n│               CI: `init <path> --name N --pin-tenant ID --pin-subscription ID   │\n│               [--repo owner/name --branch B]`.                                  │\n│ workspace     The tenants and subscriptions mov knows, one workspace each.      │\n│               `list` shows them, `use <name>` switches, `forget <name>` drops   │\n│               one. What az points at is `mov use`.                              │\n│ subscription  The subscriptions this workspace manages, inside its one tenant.  │\n│               `list` shows them, `add` declares another, `pin` chooses which is │\n│               current, `show` prints it, `rename` renames it in Azure, `remove` │\n│               stops managing one. What a subscription carries on its own -- its │\n│               budgets, declared in subscription.json and outliving every        │\n│               environment -- is `plan`, `up` and `down`.                        │\n│ use           Point az at the subscription this workspace pins, and say who is  │\n│               signed in.                                                        │\n│ check         Check tooling, identity, subscription and providers without       │\n│               changing anything: `check [env] [--fix]`.                         │\n│ update        Install the newest release, `update`; `--check` only says whether │\n│               there is one.                                                     │\n│ uninstall     Remove mov, `uninstall [-y]`: the program, this user's config,    │\n│               and the line that puts it on PATH.                                │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Profiles ──────────────────────────────────────────────────────────────────────┐\n│ new           Build a profile from the catalogue instead of from memory: `new   │\n│               [--refresh] [--into <env>] [--write-only]`.                       │\n│ catalog       What this subscription offers, read from Azure. `refresh          │\n│               [--schemas]` fetches it, `search <words>` finds a type, `show     │\n│               <type>` prints one, `verify` checks every type.                   │\n│ profile       The profiles in this workspace. `list` shows them, `show <env>`   │\n│               prints one (`--resolved`, `--origins`), `validate [env]` checks   │\n│               one or all.                                                       │\n│ names         Preview every resource name this profile would create: `names     │\n│               <env>`.                                                           │\n│ bootstrap     Print a starting point for the provisioning script a profile      │\n│               needs: `bootstrap [--list | -o file]`.                            │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Environments ──────────────────────────────────────────────────────────────────┐\n│ plan          Show what `mov up` would change, without changing it: `plan <env> │\n│               [--stage NAME] [--from NAME]`.                                    │\n│ up            Deploy the environment: `up <env> [--stage NAME] [--from NAME]`.  │\n│ status        What is currently deployed: `status <env>`.                       │\n│ ssh           Connect to a deployed VM, using the workspace-local ssh config:   │\n│               `ssh <env> [host] [--print]`.                                     │\n│ stop          Deallocate the VMs, `stop <env>`. Compute billing stops; disks    │\n│               and IPs are kept.                                                 │\n│ start         Start the VMs again after `mov stop`: `start <env>`.              │\n│ rebuild       Delete the environment and deploy it again from the profile:      │\n│               `rebuild <env>`.                                                  │\n│ down          Delete the resource group and everything in it: `down <env> [-y]  │\n│               [--wait]`.                                                        │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Reading Azure ─────────────────────────────────────────────────────────────────┐\n│ audit         Read the workspace's tenant top down, `audit [--section NAME]     │\n│               [--all] [-o file]`: subscriptions, owners, billing, budgets,      │\n│               groups and their contents.                                        │\n│ directory     Entra ID users and groups, tenant-scoped, own lifecycle. `plan`   │\n│               says what would be created, `up` creates, `show` lists what mov   │\n│               made, `down` deletes it.                                          │\n│ billing       The bill. `transfers` lists requests sent, `request --holder      │\n│               <email>` asks someone to move their products onto yours.          │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Exporting ─────────────────────────────────────────────────────────────────────┐\n│ docs          Write the exact commands a run made, as copyable monospace        │\n│               Markdown: `docs <env> [-o file]`.                                 │\n│ templates     The ARM templates mov deployed. `export <env> [-o dir]` copies    │\n│               them with their parameter files.                                  │\n└─────────────────────────────────────────────────────────────────────────────────┘",
  "panels": [
    "Setting up",
    "Profiles",
    "Environments",
    "Reading Azure",
    "Exporting"
  ],
  "commands": [
    {
      "name": "setup",
      "panel": "Setting up",
      "summary": "Create a workspace, interactively: `setup [path]`.",
      "help": " Usage: mov setup [OPTIONS] [directory]\n\n Create a workspace, interactively: `setup [path]`.\n\n Checks the machine, signs in to Azure, asks which subscription and how things\n should be named, and writes mov.workspace.json, naming.json and defaults.json.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│   directory      <path>  Where to create the workspace. Asked for if omitted.   │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --help  -h        Show this message and exit.                                   │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "init",
      "panel": "Setting up",
      "summary": "Scaffold mov.workspace.json without prompting, for scripts and CI: `init <path> --name N --pin-tenant ID --pin-subscription ID [--repo owner/name --branch B]`.",
      "help": " Usage: mov init [OPTIONS] {directory}\n\n Scaffold mov.workspace.json without prompting, for scripts and CI: `init <path>\n --name N --pin-tenant ID --pin-subscription ID [--repo owner/name --branch B]`.\n\n naming.json and defaults.json are deliberately NOT generated: mov will not\n invent a naming convention or a default region for you. `mov profile\n validate` names both files and their schemas until you write them.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│ *    directory      <path>  Directory to turn into a workspace. [required]      │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ *  --name                      <str>  Workspace name. [required]                │\n│ *  --pin-tenant                <str>  Azure tenant id to write into the         │\n│                                       workspace.                                │\n│                                       [required]                                │\n│ *  --pin-subscription          <str>  Azure subscription id to write into the   │\n│                                       workspace, as its default target.         │\n│                                       [required]                                │\n│    --repo                      <str>  GitHub repo backing this workspace, as    │\n│                                       owner/name.                               │\n│    --branch                    <str>  Default branch of --repo.                 │\n│    --help              -h             Show this message and exit.               │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "workspace",
      "panel": "Setting up",
      "summary": "The tenants and subscriptions mov knows, one workspace each. `list` shows them, `use <name>` switches, `forget <name>` drops one. What az points at is `mov use`.",
      "help": " Usage: mov workspace [OPTIONS] COMMAND [ARGS]...\n\n The tenants and subscriptions mov knows, one workspace each. `list` shows them,\n `use <name>` switches, `forget <name>` drops one. What az points at is `mov use`.\n\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --help  -h        Show this message and exit.                                   │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Commands ──────────────────────────────────────────────────────────────────────┐\n│ list    Every workspace on this machine with its tenant and subscription; `*`   │\n│         marks the one bare commands use.                                        │\n│ use     Switch: make one workspace the one bare commands use. No name: pick     │\n│         from the list.                                                          │\n│ forget  Drop a workspace from the registry. Its files, state and keys are left  │\n│         where they are.                                                         │\n└─────────────────────────────────────────────────────────────────────────────────┘",
      "subcommands": [
        "forget",
        "list",
        "use"
      ]
    },
    {
      "name": "subscription",
      "panel": "Setting up",
      "summary": "The subscriptions this workspace manages, inside its one tenant. `list` shows them, `add` declares another, `pin` chooses which is current, `show` prints it, `rename` renames it in Azure, `remove` stops managing one. What a subscription carries on its own -- its budgets, declared in subscription.json and outliving every environment -- is `plan`, `up` and `down`.",
      "help": " Usage: mov subscription [OPTIONS] COMMAND [ARGS]...\n\n The subscriptions this workspace manages, inside its one tenant. `list` shows\n them, `add` declares another, `pin` chooses which is current, `show` prints it,\n `rename` renames it in Azure, `remove` stops managing one. What a subscription\n carries on its own -- its budgets, declared in subscription.json and outliving\n every environment -- is `plan`, `up` and `down`.\n\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --help  -h        Show this message and exit.                                   │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Commands ──────────────────────────────────────────────────────────────────────┐\n│ show    The subscription this invocation acts on: name, id, alias, tenant,      │\n│         state, who is signed in, what its offer allows, and the cores and       │\n│         public addresses left in the region.                                    │\n│ list    The subscriptions this workspace manages; `*` marks the pin. Says what  │\n│         else the tenant holds.                                                  │\n│ add     Manage another subscription from this tenant here: `add [id|name]       │\n│         [--alias prod] [--pin]`.                                                │\n│ remove  Stop managing a subscription here: `remove <alias|id|name>`.            │\n│ rename  Rename the subscription in Azure, and in this workspace: `rename [name] │\n│         [-y]`. No name: asked, with the current one shown.                      │\n│ pin     Make one of this workspace's subscriptions the current one: `pin        │\n│         [alias|id|name]`. Nothing in Azure changes.                             │\n│ plan    Say what `up` would do to the budgets on this subscription. Nothing is  │\n│         changed.                                                                │\n│ up      Put the declared budgets on this subscription: create what is missing,  │\n│         bring the rest to what is declared.                                     │\n│ down    Remove the budgets mov holds on this subscription: the ones `up`        │\n│         created or adopted, and nothing else.                                   │\n└─────────────────────────────────────────────────────────────────────────────────┘",
      "subcommands": [
        "add",
        "down",
        "list",
        "pin",
        "plan",
        "remove",
        "rename",
        "show",
        "up"
      ]
    },
    {
      "name": "use",
      "panel": "Setting up",
      "summary": "Point az at the subscription this workspace pins, and say who is signed in.",
      "help": " Usage: mov use [OPTIONS]\n\n Point az at the subscription this workspace pins, and say who is signed in.\n\n Every mov command does this on its own before its first call. `mov use` is\n for when az itself is used next, or when the pinned tenant is not signed in\n at all: `--login` signs in to it.\n\n Two things are called use. `mov use` is what az points at: the current\n workspace's pin. `mov workspace use <name>` is which workspace is current.\n\n The other tenants and subscriptions mov knows: `mov workspace list`.\n Everything the account can see, workspace or not:\n `mov audit --all --section subscriptions`.\n\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --login            Sign in to the workspace's tenant first.                     │\n│ --help   -h        Show this message and exit.                                  │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "check",
      "panel": "Setting up",
      "summary": "Check tooling, identity, subscription and providers without changing anything: `check [env] [--fix]`.",
      "help": " Usage: mov check [OPTIONS] [env]\n\n Check tooling, identity, subscription and providers without changing anything:\n `check [env] [--fix]`.\n\n `--fix` is the one exception: it adds to naming.json any pattern a newer mov\n expects and an older workspace was never given, and to mov.workspace.json\n any optional path, using the scaffold's values. Nothing present is changed.\n\n Works before a workspace exists, because that is the state a fresh install is\n in and a diagnostic that refuses to run until you are already set up is no\n diagnostic at all.\n\n `mov status` is the other question: what is deployed for one environment.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│   env      <str>  Also check this profile's location and exposure.              │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --tools            Only the tools mov runs. No Azure, no workspace.             │\n│ --fix              Repair what the workspace is short of and can be given       │\n│                    safely.                                                      │\n│ --help   -h        Show this message and exit.                                  │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "update",
      "panel": "Setting up",
      "summary": "Install the newest release, `update`; `--check` only says whether there is one.",
      "help": " Usage: mov update [OPTIONS]\n\n Install the newest release, `update`; `--check` only says whether there is one.\n\n A normal install fetches the newest published release. A development\n checkout is fast-forwarded with git instead, which is the only update\n that keeps the work in it.\n\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --check            Report only; change nothing.                                 │\n│ --yes    -y        Do not ask for confirmation.                                 │\n│ --help   -h        Show this message and exit.                                  │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "uninstall",
      "panel": "Setting up",
      "summary": "Remove mov, `uninstall [-y]`: the program, this user's config, and the line that puts it on PATH.",
      "help": " Usage: mov uninstall [OPTIONS]\n\n Remove mov, `uninstall [-y]`: the program, this user's config, and the line that\n puts it on PATH.\n\n Your workspace is not touched. It holds profiles, deployment state and the\n keys for environments that may still be running, and none of that belongs to\n mov. Its path is printed so you know where it is.\n\n Nothing in Azure is touched either. Run `mov down <env>` first for anything\n you want deleted.\n\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --yes   -y        Do not ask for confirmation.                                  │\n│ --help  -h        Show this message and exit.                                   │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "new",
      "panel": "Profiles",
      "summary": "Build a profile from the catalogue instead of from memory: `new [--refresh] [--into <env>] [--write-only]`.",
      "help": " Usage: mov new [OPTIONS]\n\n Build a profile from the catalogue instead of from memory: `new [--refresh]\n [--into <env>] [--write-only]`.\n\n Full screen: confirm which workspace, tenant and subscription it is for (and\n switch, when this machine has more than one), then fuzzy-search every resource\n type this subscription offers, pick what the project needs, watch what each pick\n brings along, answer what the picks require, and name the project. mov writes the\n profile, then offers `mov plan`, then offers `mov up` -- each its own decision.\n\n Inside: ? for help, ^n to go on, esc to go back, ^s on the last screen to write.\n Colours are your terminal's own.\n\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --refresh                    Refetch the catalogue before starting.             │\n│ --write-only                 Write the profile and stop; offer neither plan nor │\n│                              up.                                                │\n│ --into                <str>  Add to this existing profile instead of writing a  │\n│                              new one. What it already has is left as written.   │\n│ --help        -h             Show this message and exit.                        │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "catalog",
      "panel": "Profiles",
      "summary": "What this subscription offers, read from Azure. `refresh [--schemas]` fetches it, `search <words>` finds a type, `show <type>` prints one, `verify` checks every type.",
      "help": " Usage: mov catalog [OPTIONS] COMMAND [ARGS]...\n\n What this subscription offers, read from Azure. `refresh [--schemas]` fetches it,\n `search <words>` finds a type, `show <type>` prints one, `verify` checks every\n type.\n\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --help  -h        Show this message and exit.                                   │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Commands ──────────────────────────────────────────────────────────────────────┐\n│ refresh  Ask the subscription what it offers and record the answer.             │\n│ search   Find a resource type without knowing what Azure calls it.              │\n│ verify   Run every type through the picker and hold the result to its own       │\n│          schema.                                                                │\n│ show     One type in full: versions, scope, what its name must satisfy, and     │\n│          what                                                                   │\n│          it brings along -- each dependency labelled with how it is known.      │\n└─────────────────────────────────────────────────────────────────────────────────┘",
      "subcommands": [
        "refresh",
        "search",
        "show",
        "verify"
      ]
    },
    {
      "name": "profile",
      "panel": "Profiles",
      "summary": "The profiles in this workspace. `list` shows them, `show <env>` prints one (`--resolved`, `--origins`), `validate [env]` checks one or all.",
      "help": " Usage: mov profile [OPTIONS] COMMAND [ARGS]...\n\n The profiles in this workspace. `list` shows them, `show <env>` prints one\n (`--resolved`, `--origins`), `validate [env]` checks one or all.\n\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --help  -h        Show this message and exit.                                   │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Commands ──────────────────────────────────────────────────────────────────────┐\n│ list      List the profiles in this workspace.                                  │\n│ show      Print a profile as authored, or as mov resolves it.                   │\n│ validate  Resolve and strictly validate profiles. Exits non-zero on the first   │\n│           failure.                                                              │\n└─────────────────────────────────────────────────────────────────────────────────┘",
      "subcommands": [
        "list",
        "show",
        "validate"
      ]
    },
    {
      "name": "names",
      "panel": "Profiles",
      "summary": "Preview every resource name this profile would create: `names <env>`.",
      "help": " Usage: mov names [OPTIONS] {env}\n\n Preview every resource name this profile would create: `names <env>`.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│ *    env      <str>  Environment name, e.g. prod. [required]                    │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --set           <str>  Override a value: dotted.path=value (JSON or string).    │\n│                        Repeatable.                                              │\n│ --help  -h             Show this message and exit.                              │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "bootstrap",
      "panel": "Profiles",
      "summary": "Print a starting point for the provisioning script a profile needs: `bootstrap [--list | -o file]`.",
      "help": " Usage: mov bootstrap [OPTIONS] [name]\n\n Print a starting point for the provisioning script a profile needs: `bootstrap\n [--list | -o file]`.\n\n mov clones your repository onto the host and runs one script as root. That\n script is yours: copy this into your repository and edit it. mov runs what is\n in your repository, never this copy.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│   name      <str>  Which reference script. Omit to use the only one, or see     │\n│                    --list.                                                      │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --out   -o      <path>  Write here instead of printing to stdout.               │\n│ --list                  List the reference scripts and exit.                    │\n│ --help  -h              Show this message and exit.                             │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "plan",
      "panel": "Environments",
      "summary": "Show what `mov up` would change, without changing it: `plan <env> [--stage NAME] [--from NAME]`.",
      "help": " Usage: mov plan [OPTIONS] {env}\n\n Show what `mov up` would change, without changing it: `plan <env> [--stage NAME]\n [--from NAME]`.\n\n Takes the same `--stage` and `--from` as `up`, so you can preview exactly\n the run you are about to make rather than a different one.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│ *    env      <str>  Environment name, e.g. prod. [required]                    │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --stage          <str>  Only this stage.                                        │\n│ --from           <str>  Start from this stage.                                  │\n│ --set            <str>  Override a value: dotted.path=value (JSON or string).   │\n│                         Repeatable.                                             │\n│ --help   -h             Show this message and exit.                             │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "up",
      "panel": "Environments",
      "summary": "Deploy the environment: `up <env> [--stage NAME] [--from NAME]`.",
      "help": " Usage: mov up [OPTIONS] {env}\n\n Deploy the environment: `up <env> [--stage NAME] [--from NAME]`.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│ *    env      <str>  Environment name, e.g. prod. [required]                    │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --stage          <str>  Only this stage.                                        │\n│ --from           <str>  Start from this stage.                                  │\n│ --set            <str>  Override a value: dotted.path=value (JSON or string).   │\n│                         Repeatable.                                             │\n│ --help   -h             Show this message and exit.                             │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "status",
      "panel": "Environments",
      "summary": "What is currently deployed: `status <env>`.",
      "help": " Usage: mov status [OPTIONS] {env}\n\n What is currently deployed: `status <env>`.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│ *    env      <str>  Environment name, e.g. prod. [required]                    │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --help  -h        Show this message and exit.                                   │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "ssh",
      "panel": "Environments",
      "summary": "Connect to a deployed VM, using the workspace-local ssh config: `ssh <env> [host] [--print]`.",
      "help": " Usage: mov ssh [OPTIONS] {env} [host]\n\n Connect to a deployed VM, using the workspace-local ssh config: `ssh <env> [host]\n [--print]`.\n\n `~/.ssh` is never involved: the key and the host entry both live in the\n workspace, so this works on a machine that has never seen this environment\n and leaves nothing behind on one that has.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│ *    env       <str>  Environment name, e.g. prod. [required]                   │\n│      host      <str>  VM purpose, e.g. web. Defaults to the only one.           │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --print            Print the command instead of running it.                     │\n│ --help   -h        Show this message and exit.                                  │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "stop",
      "panel": "Environments",
      "summary": "Deallocate the VMs, `stop <env>`. Compute billing stops; disks and IPs are kept.",
      "help": " Usage: mov stop [OPTIONS] {env}\n\n Deallocate the VMs, `stop <env>`. Compute billing stops; disks and IPs are kept.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│ *    env      <str>  Environment name, e.g. prod. [required]                    │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --help  -h        Show this message and exit.                                   │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "start",
      "panel": "Environments",
      "summary": "Start the VMs again after `mov stop`: `start <env>`.",
      "help": " Usage: mov start [OPTIONS] {env}\n\n Start the VMs again after `mov stop`: `start <env>`.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│ *    env      <str>  Environment name, e.g. prod. [required]                    │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --help  -h        Show this message and exit.                                   │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "rebuild",
      "panel": "Environments",
      "summary": "Delete the environment and deploy it again from the profile: `rebuild <env>`.",
      "help": " Usage: mov rebuild [OPTIONS] {env}\n\n Delete the environment and deploy it again from the profile: `rebuild <env>`.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│ *    env      <str>  Environment name, e.g. prod. [required]                    │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --yes   -y             Do not ask for confirmation.                             │\n│ --set           <str>  Override a value: dotted.path=value (JSON or string).    │\n│                        Repeatable.                                              │\n│ --help  -h             Show this message and exit.                              │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "down",
      "panel": "Environments",
      "summary": "Delete the resource group and everything in it: `down <env> [-y] [--wait]`.",
      "help": " Usage: mov down [OPTIONS] {env}\n\n Delete the resource group and everything in it: `down <env> [-y] [--wait]`.\n\n This is a nuke, not a pause. Use `mov stop` to stop paying for compute while\n keeping the machines, their disks and their keys.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│ *    env      <str>  Environment name, e.g. prod. [required]                    │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --yes   -y                 Do not ask for confirmation.                         │\n│ --wait      --no-wait      Block until the group is gone. [default: no-wait]    │\n│ --help  -h                 Show this message and exit.                          │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "audit",
      "panel": "Reading Azure",
      "summary": "Read the workspace's tenant top down, `audit [--section NAME] [--all] [-o file]`: subscriptions, owners, billing, budgets, groups and their contents.",
      "help": " Usage: mov audit [OPTIONS]\n\n Read the workspace's tenant top down, `audit [--section NAME] [--all] [-o file]`:\n subscriptions, owners, billing, budgets, groups and their contents.\n\n Read-only. Nothing here deploys, changes or deletes anything. Scoped to the\n pinned tenant; `--all` reads every tenant this account can see.\n\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --section          <str>            Run only this section. See --list.          │\n│ --list                              List the sections and exit.                 │\n│ --all                               Every tenant and subscription this account  │\n│                                     can see, not only the workspace's.          │\n│ --format   -f      <table|md|json>  How to present the report. [default: table] │\n│ --out      -o      <path>           Write the report to this file instead of    │\n│                                     the terminal.                               │\n│ --help     -h                       Show this message and exit.                 │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "directory",
      "panel": "Reading Azure",
      "summary": "Entra ID users and groups, tenant-scoped, own lifecycle. `plan` says what would be created, `up` creates, `show` lists what mov made, `down` deletes it.",
      "help": " Usage: mov directory [OPTIONS] COMMAND [ARGS]...\n\n Entra ID users and groups, tenant-scoped, own lifecycle. `plan` says what would\n be created, `up` creates, `show` lists what mov made, `down` deletes it.\n\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --help  -h        Show this message and exit.                                   │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Commands ──────────────────────────────────────────────────────────────────────┐\n│ show  List the Entra ID objects mov has created in this tenant.                 │\n│ plan  Say which people and groups would be created. Nothing is changed.         │\n│ up    Create the people and groups, on their own, with no environment in the    │\n│       way.                                                                      │\n│ down  Delete every Entra ID user and group mov created. Separate from `mov      │\n│       down` on purpose.                                                         │\n└─────────────────────────────────────────────────────────────────────────────────┘",
      "subcommands": [
        "down",
        "plan",
        "show",
        "up"
      ]
    },
    {
      "name": "billing",
      "panel": "Reading Azure",
      "summary": "The bill. `transfers` lists requests sent, `request --holder <email>` asks someone to move their products onto yours.",
      "help": " Usage: mov billing [OPTIONS] COMMAND [ARGS]...\n\n The bill. `transfers` lists requests sent, `request --holder <email>` asks\n someone to move their products onto yours.\n\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --help  -h        Show this message and exit.                                   │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Commands ──────────────────────────────────────────────────────────────────────┐\n│ plan       Say which billing identities would be created and which role each    │\n│            would hold. Nothing is changed.                                      │\n│ up         Create the billing identities and grant each the role it declares.   │\n│ show       The billing identities mov created in this tenant, and the role each │\n│            holds.                                                               │\n│ down       Delete the billing identities mov created, and the roles they were   │\n│            granted.                                                             │\n│ matrix     Ask Azure what each billing identity is actually allowed to do.      │\n│ transfers  List the requests this invoice section has sent asking for products. │\n│ request    Ask someone to move their Azure products onto your bill.             │\n└─────────────────────────────────────────────────────────────────────────────────┘",
      "subcommands": [
        "down",
        "matrix",
        "plan",
        "request",
        "show",
        "transfers",
        "up"
      ]
    },
    {
      "name": "docs",
      "panel": "Exporting",
      "summary": "Write the exact commands a run made, as copyable monospace Markdown: `docs <env> [-o file]`.",
      "help": " Usage: mov docs [OPTIONS] {env}\n\n Write the exact commands a run made, as copyable monospace Markdown: `docs <env>\n [-o file]`.\n\n Not a reconstruction: this is the transcript recorded while the deployment\n actually happened, redacted through the workspace's docs.redact rules.\n\n┌─ Arguments ─────────────────────────────────────────────────────────────────────┐\n│ *    env      <str>  Environment name, e.g. prod. [required]                    │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --out   -o      <path>  Where to write. Defaults to <docs>/<env>/commands.md.   │\n│ --help  -h              Show this message and exit.                             │\n└─────────────────────────────────────────────────────────────────────────────────┘"
    },
    {
      "name": "templates",
      "panel": "Exporting",
      "summary": "The ARM templates mov deployed. `export <env> [-o dir]` copies them with their parameter files.",
      "help": " Usage: mov templates [OPTIONS] COMMAND [ARGS]...\n\n The ARM templates mov deployed. `export <env> [-o dir]` copies them with their\n parameter files.\n\n┌─ Options ───────────────────────────────────────────────────────────────────────┐\n│ --help  -h        Show this message and exit.                                   │\n└─────────────────────────────────────────────────────────────────────────────────┘\n┌─ Commands ──────────────────────────────────────────────────────────────────────┐\n│ export  Copy the templates and parameter files a run actually deployed.         │\n└─────────────────────────────────────────────────────────────────────────────────┘",
      "subcommands": [
        "export"
      ]
    }
  ]
};
